import { WhatsAppConnectionPage } from '../features/whatsapp/WhatsAppConnectionPage';

// Kept as the routed page name so existing navigation remains valid. Phase 1
// intentionally exposes only the WhatsApp Business connection vertical slice.
export const WhatsappCampaign = WhatsAppConnectionPage;

export default WhatsappCampaign;